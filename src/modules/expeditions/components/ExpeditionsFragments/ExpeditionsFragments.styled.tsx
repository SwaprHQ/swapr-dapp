import styled from 'styled-components'

import { Base } from '../../../../components/Button/Button'

export const FragmentsBalance = styled(Base)`
  background: linear-gradient(180deg, #131218, #1d184b);
  border: 1px solid #464366;
  backdrop-filter: blur(12px);
  padding: 8px 14px;
  width: auto;
  font-size: 18px;
`
