import { DAI, WETH } from '@swapr/sdk'

import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { CurrencyLogoComponent } from './CurrencyLogo.component'

import { NATIVE_CURRENCY_LOGO } from './CurrencyLogo.utils'

<Meta title="Currencies" component={CurrencyLogoComponent} />

# CurrencyLogoComponent

Show the currency logo.

<Canvas>
  <Story
    name="CurrencyLogoComponent"
    argTypes={{
      sources: {
        control: 'select',
        options: [[NATIVE_CURRENCY_LOGO[1]], [NATIVE_CURRENCY_LOGO[100]]],
      },
    }}
    args={{ currency: WETH[1], sources: [NATIVE_CURRENCY_LOGO[1]], size: '48px' }}
  >
    {CurrencyLogoComponent.bind()}
  </Story>
</Canvas>

<ArgsTable of={CurrencyLogoComponent} />
